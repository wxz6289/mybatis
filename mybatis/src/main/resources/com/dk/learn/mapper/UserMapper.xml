<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dk.learn.mapper.UserMapper">
    <resultMap id="userResultMap" type="com.dk.learn.entity.User">
        <id property="createdTime" column="createdAt"/>
        <id property="updatedTime" column="updatedAt"/>
    </resultMap>
    <delete id="batchRemoveUsers">
        DELETE FROM user WHERE id IN
        <foreach item="id" collection="ids" separator="," open="(" close=")">
            #{id}
        </foreach>
    </delete>
    <select id="list" resultMap="userResultMap">
        SELECT * FROM user
        <where>
            <if test="name != null and name != ''">
                AND name LIKE CONCAT('%', #{name}, '%')
            </if>
            <if test="startAge != null">
                AND age &gt;= #{startAge}
            </if>
            <if test="endAge != null">
                AND age &lt;= #{endAge}
            </if>
        </where>
        ORDER BY createdAt DESC
        <if test="offset != null and size != null">
            LIMIT #{offset}, #{size}
        </if>
    </select>
    <select id="updateUserInfo" resultMap="userResultMap">
        UPDATE user
        <set>
            <if test="name != null">
                name = #{name},
            </if>
            <if test="age != null">
                age = #{age},
            </if>
            <if test="deptId != null">
                deptId = #{deptId},
            </if>
        </set>
        WHERE id = #{id}
    </select>
</mapper>