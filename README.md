# MyBatis 学习工程（Spring Boot）

本仓库为 **Maven 单模块** Spring Boot + MyBatis 示例工程，根目录即工程根（`pom.xml`、`src/`、`doc/`）。

## 本地运行

按仓库根目录 `.env.example` 配置环境变量后：

```bash
./mvnw spring-boot:run
```

详见 `doc/QUICK_START.md`（若存在）。
